name: doctl

on:
  push:
    branches: [ master ]
    paths:
      - 'Dockerfile'
      - '.github/workflows/**'

env:
  IMAGE_NAME: frontend

jobs:
  build:
    
    name: Build, push, and deploy
    runs-on: ubuntu-latest
    steps:

    - name: Get current date time    
      id: date
      run: echo "::set-output name=date::$(date +'%Y-%m-%d--%H%M')"
    
    - name: Checkout master
      uses: actions/checkout@master

    - name: Build image
      run: docker build -t frontend-${{ steps.date.outputs.date }} .
    
    - name: Log into registry
      run: echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
    
    - name: Push to GHCR
      run: docker push ghcr.io/shrestharajat/frontend-${{ steps.date.outputs.date }}

#     - name: Push image
#         run: |
#           IMAGE_ID=docker.pkg.github.com/${{ github.repository }}/frontend-${{ steps.date.outputs.date }}
          
#           # Change all uppercase to lowercase
#           IMAGE_ID=$(echo $IMAGE_ID | tr '[A-Z]' '[a-z]')
#           # Strip git ref prefix from version
#           VERSION=$(echo "${{ github.ref }}" | sed -e 's,.*/\(.*\),\1,')
#           # Strip "v" prefix from tag name
#           [[ "${{ github.ref }}" == "refs/tags/"* ]] && VERSION=$(echo $VERSION | sed -e 's/^v//')
#           # Use Docker `latest` tag convention
#           [ "$VERSION" == "master" ] && VERSION=latest
#           echo IMAGE_ID=$IMAGE_ID
#           echo VERSION=$VERSION
#           docker tag $IMAGE_NAME $IMAGE_ID:$VERSION
#           docker push $IMAGE_ID:$VERSION
